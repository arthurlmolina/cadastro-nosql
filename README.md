O README e a documentação permitem compreender, instalar, executar e testar a PoC, apresentando as tecnologias utilizadas,
a estrutura do banco de dados e o funcionamento da API. O projeto foi desenvolvido em **Java**, utilizando **MongoDB** para 
armazenamento dos dados e **Docker** para facilitar a configuração e execução da aplicação. A solução é voltada à **empregabilidade** 
e está relacionada ao **ODS 8 – Trabalho Decente e Crescimento Econômico**. A aplicação é executada **localmente por meio do servidor 
Tomcat**, disponibilizando uma **API REST** para realizar as operações do sistema. A API possui endpoints para **criação, consulta,
alteração e exclusão** de registros. Para testar as funcionalidades, basta iniciar a aplicação e realizar as requisições utilizando
a URL local da API, sem necessidade de senha ou autenticação. As requisições podem ser realizadas por ferramentas como **Postman**,
informando o método HTTP correspondente ao endpoint e, quando necessário, os dados no formato **JSON**. Dessa forma, é possível testar 
as operações de cadastro, consulta, atualização e exclusão e verificar os resultados diretamente no **MongoDB**.
